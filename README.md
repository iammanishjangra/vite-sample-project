# Vite Docker Sample

## Run locally

```bash
npm install
npm run dev
```

App will run on `http://localhost:4700`.

## Run with Docker

```bash
docker build -t vite-docker-sample .
docker run --rm -p 4700:4700 vite-docker-sample
```
Hello Sir
Hello
